package stage_1.week_2.solo_finance_tracker

import java.time.LocalDate
import java.util.*

/*
enum class TransactionType {
    INCOME, EXPENSE
}

data class Transaction(
    val id: UUID = UUID.randomUUID(),
    val amount: Double,
    val date: LocalDate,
    val category: String,
    val type: TransactionType
)

class TransactionManager(private val storage: Storage) {
    fun addTransaction(transaction: Transaction) = storage.add(transaction)

    fun editTransaction(id: UUID, editTransaction: Transaction): Boolean =
        storage.edit(id, editTransaction)

    fun deleteTransaction(id: UUID): Boolean = storage.delete(id)

    fun getAllTransactions(): List<Transaction> = storage.getAll()
}*/
